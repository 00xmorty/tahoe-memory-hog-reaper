name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests
        run: bash tests/test_tahoe_memory_hog_reaper.sh
